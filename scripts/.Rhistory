dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
# Добавление названия центрирующего гена
centered_data$center_gene_name <- center_gene_name
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene, center_gene_name)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = ifelse(is.na(gene), "ALP", gene)),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Название центрирующего гена
center_gene_name <- "ALP"
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, center_gene_name, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
# Добавление названия центрирующего гена
centered_data$center_gene_name <- center_gene_name
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene, center_gene_name)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = if_else(gene == center_gene, "ALP", gene)),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Замена NA на "ALP" только для центрирующего гена
all_centered_genes$gene <- if_else(is.na(all_centered_genes$gene) & all_centered_genes$file_id == "1", "ALP", all_centered_genes$gene)
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 100000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Замена NA на "ALP" только для центрирующего гена
all_centered_genes$gene <- if_else(is.na(all_centered_genes$gene) & all_centered_genes$file_id == "1", "ALP", all_centered_genes$gene)
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Замена NA на "ALP" только для центрирующего гена
all_centered_genes$gene <- if_else(is.na(all_centered_genes$gene) & all_centered_genes$file_id == "1", "ALP", all_centered_genes$gene)
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Замена NA на "ALP" для всех центрирующих генов
all_centered_genes$gene <- if_else(is.na(all_centered_genes$gene), "ALP", all_centered_genes$gene)
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_fill_manual(values = c("ALP" = "red", "other_genes" = "blue")) +  # Яркий цвет для "ALP"
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
# Определение центрирующего гена
center_gene <- center_gene_list[1]
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Замена NA на имя центрирующего гена
all_centered_genes$gene <- ifelse(is.na(all_centered_genes$gene), center_gene, all_centered_genes$gene)
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = paste("Гены вокруг центрирующего гена", center_gene, "(центрированные координаты)"))
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
# Создание графика с выделением центрирующего гена
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = if_else(gene == center_gene, "red", "blue")),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно metG
center_coordinates <- function(gtf_data, metG_coords, window_size = 10000) {
metG_coords <- metG_coords %>% dplyr::select(seqid, metG_start = start, metG_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% metG_coords$seqid) %>%
mutate(centered_start = start - metG_coords$metG_start,
centered_end = end - metG_coords$metG_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
metG_genes_list <- lapply(gtf_files, function(file) {
gtf <- rtracklayer::readGFF(file)
metG_coords <- gtf %>%
filter(type == "CDS", gene == "metG") %>%
dplyr::select(seqid, start, end)
centered_genes <- center_coordinates(gtf, metG_coords)
return(centered_genes)
})
# Объединение данных из всех файлов
all_metG_genes <- bind_rows(metG_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_metG_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.7) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг metG (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.7) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gsub(".gtf", "", basename(gtf_files))) +
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
strain_names <- gsub("_GCF.*", "", final_result$strain)  # Извлекаем только имя штамма
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = strain_names) +  # Используем имена штаммов
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
library(tidyverse)
library(GenomicFeatures)
library(ggbio)
# Получение списка файлов GTF в папке
gtf_files <- final_result$filename
# Функция для центрирования координат относительно указанного гена
center_coordinates <- function(gtf_data, center_gene, window_size = 10000) {
center_gene_coords <- gtf_data %>%
filter(type == "CDS", gene_id == center_gene) %>%
dplyr::select(seqid, center_start = start, center_end = end)
centered_data <- gtf_data %>%
filter(type == "CDS",
seqid %in% center_gene_coords$seqid) %>%
mutate(centered_start = start - center_gene_coords$center_start,
centered_end = end - center_gene_coords$center_start,
direction = if_else(centered_start < centered_end, "->", "<-")) %>%
filter(centered_end >= -window_size, centered_start <= window_size) %>%
dplyr::select(gene, product, centered_start, centered_end, direction)
return(centered_data)
}
# Считывание и центрирование данных
center_gene_list <- final_result$gene_id
centered_genes_list <- lapply(seq_along(gtf_files), function(i) {
file <- gtf_files[i]
center_gene <- center_gene_list[i]
gtf <- rtracklayer::readGFF(file)
centered_genes <- center_coordinates(gtf, center_gene)
return(centered_genes)
})
# Объединение данных из всех файлов
all_centered_genes <- bind_rows(centered_genes_list, .id = "file_id")
# Изменение лейблов по вертикали
gtf_files_labels <- gsub("_GCF_.*", "", basename(gtf_files))
# Создание графика
p <- ggplot() +
geom_rect(data = all_centered_genes,
aes(xmin = centered_start, xmax = centered_end, ymin = as.numeric(file_id) - 0.4, ymax = as.numeric(file_id) + 0.4, fill = gene),
alpha = 0.5) +
scale_y_continuous(breaks = seq_along(gtf_files), labels = gtf_files_labels) +  # Используем измененные лейблы
theme_minimal() +
labs(title = "Гены вокруг указанного центрирующего гена (центрированные координаты)")
# Вывод графика
p
